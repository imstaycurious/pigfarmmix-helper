# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / IDE
.claude/
.windsurf/
.vscode/
.idea/
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Local secrets
.env
.dev.vars

# Scratch / scraped artifacts (if they come back)
*.tmp
*.log

*.py
.wrangler/
*/.wrangler/
tmp*
pig143*
static/img/png_h/*

# 本地保留的完整数据（含已剔除字段）；提交版是精简版
static/data/*.full.json
# 本地排版查看用的 pretty JSON
static/data/*.pretty.json# 数据备份文件
static/data/*.backup
static/data/*_v2_old.json

# TS 编译相关 (保留 static 下的编译产物以支持直接部署)

# Dependencies
node_modules/

# 编译产物 (dist/ 由 CI 构建, 不提交)
dist/
# static 下的旧编译产物 (已迁移到 dist/, 不再跟踪)
static/app.js
static/sw.js
static/js/
static/render/
